# توثيق نظام تتبع نقرات الواتساب (WhatsApp Click Tracking)

## 1. كيف يعمل نظام التتبع (Tracking Architecture)

يعتمد النظام على بنية **Backend Redirection Tracking** لضمان عدم ضياع أي نقرة:

1. **الضغط في واجهة المستخدم (User Click):**
   عند نقر المستخدم على أي رابط أو زر واتساب في الموقع، يتوجه الرابط إلى نقطة نهاية داخلية مؤمنة:
   `/go/whatsapp/:cta?accountId=...&sourcePage=...&sessionId=...&visitorId=...&utm_source=...`

2. **معالجة الحدث على الخادم (Server Processing):**
   * التحقق من سلامة الموضع (CTA Validation) والوجهة المسموح بها (Whitelist).
   * استخراج بيانات الجهاز (Mobile, Desktop, Tablet) والمتصفح ونظام التشغيل.
   * تجزئة عنوان IP باستخدام Salt سري (SHA-256 HMAC) لحماية الخصوصية.
   * فحص معدل الطلبات (Rate Limiting) واكتشاف حركة البوتات (Bot Detection).
   * فحص تكرار النقرات السريعة (Deduplication within 5 seconds window).
   * تسجيل الحدث في جدول `whatsapp_click_events` وتحديث التجميع اليومي `whatsapp_click_daily_aggregates`.

3. **التحويل السريع (Safe Fast Redirect):**
   * يقوم الخادم بإرجاع استجابة `HTTP 302 Found` الفورية مع الرابط المعتمد `https://wa.me/...`.
   * إضافة ترويسات `X-Robots-Tag: noindex, nofollow` و `Cache-Control: no-store` لمنع فهرسة روابط التتبع أو تخزينها.
   * **خاصية الأمان ضد الأعطال (Fallback Resilience):** في حال حدوث أي خطأ في قاعدة البيانات، لا يتعطل المستخدم بل يتم إرسال التحويل إلى الواتساب فوراً.

---

## 2. كيفية إضافة موضع واتساب جديد (How to Add a New CTA)

باستخدام المكوّن الموحد `TrackedWhatsAppLink`:

```tsx
import { TrackedWhatsAppLink } from "@/components/TrackedWhatsAppLink";

<TrackedWhatsAppLink
  cta="floating_button" // اسم الموضع التعريفي
  accountId={account?.id} // اختياري لربط المنتج
  text="مرحباً، لدي استفسار عن الحسابات" // رسالة مسبقة اختيارية
  className="my-button-style"
>
  تواصل عبر الواتساب
</TrackedWhatsAppLink>
```

---

## 3. ربط النقرة بمنتج (Product Attribution)

عند تمرير `accountId` أو `accountSlug`:
* يقوم الخادم بالتقاط لقطة تاريخية (Snapshot) لبيانات الحساب:
  - اسم المنتج (`account_title`)
  - نوع اللعبة (`account_game`: CoC أو CR)
  - مستوى التاون هول (`account_town_hall`)
  - السعر الحالي (`account_price`)
* وبذلك تظل التحليلات دقيقة حتى لو تم تعديل الحساب أو حذفه لاحقاً.

---

## 4. منع التضخم الوهمي للنقرات (Event Deduplication)

* إذا نقر المستخدم عدة مرات متتالية بسرعة (أقل من 5 ثوانٍ) لنفس الجلسة والمنتج والزر:
  - يتم تسجيل الحدث، ولكن مع تعيين `is_unique_click = false`.
* في لوحة التحكم يتم عرض:
  - **إجمالي الضغطات (Total Clicks)**
  - **الضغطات الفريدة (Unique Clicks)**
  - **الزوار الفريدون (Unique Visitors)**
  - **الجلسات الفريدة (Unique Sessions)**

---

## 5. سياسة الخصوصية وحماية البيانات (Privacy by Design)

### ما يتم تخزينه:
* معرف الجلسة المجهول (`sessionId`) ومعرف الزائر (`visitorId`).
* لقطة الحساب (ID, Title, Game, Price, Town Hall).
* موضع الزر والصفحة المصدر والمرجع (Referrer).
* معاملات الحملات الإعلانية (`utm_source`, `utm_medium`, `utm_campaign`, `utm_content`, `utm_term`).
* تصنيف الجهاز التقريبي (الجوال، الكمبيوتر، اللوحي) والمتصفح ونظام التشغيل.
* الدولة المشتقة من ترويسات Cloudflare Edge (`CF-IPCountry`).
* التجزئة المشفرة للـ IP (`ip_hash`) عبر `HMAC-SHA256(IP, TRACKING_HASH_SECRET)`.
* نوع الزيارة (`human`, `bot`, `suspicious`).

### ما لا يتم تخزينه إطلاقاً:
* ❌ عنوان IP الخام للزائر (Zero raw IP storage).
* ❌ رقم هاتف المستخدم أو هويته الشخصية.
* ❌ محتوى محادثات أو رسائل الواتساب.
* ❌ أي بصمات متصفح معقدة أو غازية للخصوصية.

---

## 6. الحماية والأمان (Security & Abuse Prevention)

* **منع التحويل المفتوح (Open Redirect Prevention):** الروابط تقبل التوجيه حصراً إلى نطاق الواتساب المعتمد `https://wa.me/` مع الرقم المعتمد للمتجر. يُمنع قبول أي رابط خارجي عشوائي من العميل.
* **تحديد المعدل (Rate Limiting):** حد أقصى 40 نقرة بالدقيقة لكل IP Hash، وعند تجاوزه يتم وسم الزيارات كـ `suspicious` دون منع الزبون الحقيقي من الشراء.
* **فلترة البوتات (Bot Detection):** فحص ترويسات الـ User-Agent وتصنيف محركات البحث وعناكب الفحص تلقائياً.

---

## 7. قراءة واستخدام التحليلات (Admin Analytics)

يمكن للأدمن الوصول إلى صفحة **تحليلات الواتساب** من القائمة الجانبية في لوحة التحكم (`/admin/whatsapp`):
* **بطاقات الأداء الرئيسية (KPIs):** إجمالي النقرات، الزوار الفريدون، المنتج الأكثر طلباً، وأفضل مصدر زيارات.
* **الرسم البياني الزمني:** فترات متعددة (اليوم، أمس، آخر 7 أيام، آخر 30 يوماً، هذا الشهر) مع تبديل مقياس العرض.
* **أداء مواضع الأزرار (CTA Breakdown):** لمقارنة أداء أزرار الكروت مقابل الفوتر وتفاصيل المنتج.
* **أداء المنتجات (Product Breakdown):** معرفة الحسابات التي تجلب محادثات شراء فعلية.
* **أوقات الذروة:** تحليل توزيع الساعات (00:00 - 23:00) وأيام الأسبوع.
* **تصدير التقارير:** تصدير كامل البيانات المطابقة للفلاتر إلى ملف CSV بنقرة واحدة مع دعم اللغة العربية.
